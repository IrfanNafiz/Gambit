//void setangle(int angle, myservo) {
//  myservo.easeTo(angle);                  // sets the servo position according to the scaled value and using easing functions
//  delay(15);                           // waits for the servo to get there
//}
//
//void idle() {
//  setangle(0, servo);
//  setangle(0, servoarm1);
//  setangle(0, servoarm2;
//  setangle(0, servoclaw);
//}
